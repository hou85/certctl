apiVersion: v1
kind: Service
metadata:
  name: certctl
  namespace: {{ .Release.Namespace }}

spec:
  selector:
    app: certctl

  ports:
    - name: https
      port: 443
      targetPort: 8080
      protocol: TCP