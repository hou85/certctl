image:
  repository: ghcr.io/certctl-io/certctl-server
  tag: v2.0.9
  pullPolicy: Always

server:
  auth:
    apiKey: ""           # OBLIGATOIRE — passe via --set ou values override
    bootstrapToken: ""   # Optionnel, généré aléatoirement si vide
service:
  port: 80

postgres:
  image: postgres:16
  database: certctl
  username: certctl
  password: change-me
  storage: 10Gi

tailscale:
  hostname: certctl
  tailnet: tail44be45.ts.net
