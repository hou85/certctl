apiVersion: v1
kind: Service
metadata:
  name: dex
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: dex
  ports:
    - name: http
      port: 80
      targetPort: 5556
      protocol: TCP
      appProtocol: http
